{
  "mcpServers": {
    "discord": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/index.js"],
      "env": {
        "DISCORD_BOT_TOKEN": "${DISCORD_BOT_TOKEN}"
      }
    }
  }
}
